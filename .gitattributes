# ============================================================
#  行尾策略（作为发布源必须钉死，不能依赖 clone 方的 git 配置）
# ============================================================
#
# [!] 为什么必须有这个文件：
#     本项目实测过一次「双击启动器秒退」——窗口在打印任何东西之前就消失。
#     成因之一就是 .bat 行尾不是纯 CRLF。
#     而 git 的 core.autocrlf 是【每台机器自己的设置】：
#       Windows 默认 true  → checkout 出 CRLF，碰巧没事
#       Linux/macOS 默认 false → checkout 出 LF  → bat 直接坏
#     所以必须在仓库里用 eol=crlf 钉死。
#
# [!] 规则顺序有意义：同一属性【后匹配的规则覆盖先匹配的】。
#     所以通配兜底必须写在前面，具体规则写在后面。

# ---- 兜底：其余文本文件一律 LF ----
* text=auto eol=lf

# ---- Windows 批处理：强制 CRLF（覆盖上面的兜底）----
# 裸 LF 的 .bat 在「从批处理文件调用」时会让整个括号块解析中止，
# 表现为双击瞬退、stdout 0 字节。
*.bat text eol=crlf
*.cmd text eol=crlf

# ---- 运行时缓存：按二进制处理，不做任何行尾转换 ----
# （本来就被 .gitignore 排除，这里是双保险）
*.cache.json -text
*.cache.txt  -text
